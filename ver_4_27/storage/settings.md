
Compiling to Google Android
```
Project Settings\
	Platforms\
		Android\
			Configure now
			Accept SDK License
			[dev_checkDef ? Install Location]
			n Allow large OBB files -- sets limit from 2GB : required by Google Play Store ; to 4GB : .zip max .
			[dev if size > 2.1GB ? Allow patch OBB file]
			[dev if size > 2.1GB ? Allow overflow OBB files]
			Orientation
			Enable FullScreen Immersive on KitKat and above devices
		Android SDK\
			[_]
```
